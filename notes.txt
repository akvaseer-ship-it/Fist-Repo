git notes using of the commands notes 

git --verstion
git config --global user.name "akvaseer"
git config --global user.email "akvaseer@gmail.com"
git config --list
git clone https://github.com/akvaseer
git status
git add "file name"
git add .
git commit -m "Some message"
git push origin mian
git init
git remote add origin <link past>
git remote -v  (to verify remote)
git branch  (to check branch)
git branch -m main (to rename branch)
git push origin main
git push -u origin main

# Explain Branches
git branch (to check branch)
git branch -m main (to rename branch)
git checkout <branch name> (to navicate)
git checkout -b <New Branch name> (to create new branch)
git branch -d <Branch Name> (to delete branch)
git diff <Branch Name> (to see the diffirences)
git merge main (to merge to branches) one way to create

other to merge brach is pull request 
